services:
  - type: web
    name: instant-print-kiosk
    env: node
    buildCommand: npm run install:all && npm run build
    startCommand: npm start
    region: singapore
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
